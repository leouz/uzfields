= render layout: "#{path}/form/wrapper/with_label", locals: { options: options } do
  textarea.form-control type="textarea" name="#{options[:tag_name]}" ng-model="#{options[:ng_model]}" placeholder="#{options[:placeholder] ||= ""}" rows="#{options[:rows] ||= "5"}"
