= render :layout => "fields/wrapper_for_input", :locals => { :f => f, :field_name => field_name, :options => options } do
  = f.date_select field_name
/ .input-group.datetimepicker
/     %span.input-group-addon
/       %span.glyphicon.glyphicon-calendar                    
/     = f.text_field field_name, value: f.object.send(field_name).strftime("%m/%d/%Y"), :class => 'form-control datepicker'
