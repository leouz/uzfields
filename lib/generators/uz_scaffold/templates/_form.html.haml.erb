= form_for [:admin, @<%= @model_name.underscore %>], :html => { :class => 'form-horizontal' } do |f|
<% @columns.each do |p| %>  = bs_input f, :<%= p[:name] %>, :<%= p[:input] %>
<% end %>
  = bs_actions f, admin_<%= @model_name.underscore.pluralize %>_path
  